import React from "react";
import { Link } from "react-router-dom";

const Back = () => {
    return (
        <React.Fragment>
            <Link to="/">Home</Link>
        </React.Fragment>
    );
};
export default Back;
